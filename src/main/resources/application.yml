spring:
  datasource:
    url: jdbc:mysql://localhost:3306/module02_lms
    username: Alien
    password: Alien

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  servlet:
    session:
      timeout: 24h
      cookie:
        http-only: true
        secure: false
        same-site: lax